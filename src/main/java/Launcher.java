import javafx.application.Application;

/**
 * Represents a Launcher class, used to launch the GUI.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
