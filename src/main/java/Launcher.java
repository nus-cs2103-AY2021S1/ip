import javafx.application.Application;

/**
 * Launches Duke and works around classpath issues.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
