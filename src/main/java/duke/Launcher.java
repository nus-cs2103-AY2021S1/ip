package duke;

import javafx.application.Application;

/**
 * Represent the launcher for Duke's GUI
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
