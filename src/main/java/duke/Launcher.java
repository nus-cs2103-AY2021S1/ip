package duke;

import javafx.application.Application;

/**
 * Launcher class
 * Launches the GUI for the Duke chat-bot
 */
public class Launcher {

    /**
     * Main method to launch the GUI
     * @param args unused
     */
    public static void main(String[] args) {
        Application.launch(Duke.class, args);
    }
}