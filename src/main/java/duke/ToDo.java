package duke;

/**
 * Represents the task of "todo" category
 */

public class ToDo extends Task {

    public ToDo(int category, int status, String command) {
        super(category, status, command);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}