package
        duke.command;

/**
 * The enum CommandType denotes the type of command.
 *
 * @author Alvin Chee
 */
public enum CommandType {
        BYE,
        PRINT,
        MARKTASKDONE,
        HANDLETODO,
        HANDLEDEADLINE,
        HANDLEVENT,
        DELETETASK,
        FINDMATCHINGTASK,
        DUPLICATE,
        REMOVEDUPLICATES,
        CLEAR,
        HELP,
        ERROR;
}
