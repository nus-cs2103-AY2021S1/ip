package duke.command;

/**
 * Encapsulates an add command for the Duke program. These are commands that add tasks to the task list. There is an
 * add command for each type of task, namely: to-do, deadline and event tasks.
 */
public class AddCommand extends Command {
}
