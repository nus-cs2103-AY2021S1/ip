package duke.command;

/**
 * The list of commands available for {@code Duke}.
 */
public enum CommandList {
    bye,
    exit,
    list,
    todo,
    deadline,
    event,
    done,
    delete,
    find
}
