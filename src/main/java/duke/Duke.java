package duke;

import javafx.application.Application;

public class Duke {
    // comment cos i did it, but not in a branch so this comment is part of making the branch :/

    public static void main(String[] args) {
        Application.launch(WindowDisplay.class);
    }
}
