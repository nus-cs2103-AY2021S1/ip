package duke;

/**
 * DukeExceptionType stores all types of duke-specific exceptions.
 */
public enum DukeExceptionType {
    EMPTY_LIST,
    MISSING_DESCRIPTION,
    MISSING_TIMING,
    INVALID_INDEX,
    WRONG_FORMAT,
    UNKNOWN,
}
