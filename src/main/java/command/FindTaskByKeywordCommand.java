package command;

public class FindCommand extends Command {
    public FindCommand (String ...parameter) {
        super(parameter);
    }

    @Override
    public Result execute() {
        return null;
    }
}
