/**
 * Encapsulates the different types of commands in an Enum.
 * Includes an invalid command.
 */
public enum Command {
    BYE,
    LIST,
    DONE,
    TODO,
    DEADLINE,
    EVENT,
    DELETE,
    FIND,
    INVALID
}
