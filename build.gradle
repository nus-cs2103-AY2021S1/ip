plugins {
    id "application"
}

apply plugin: 'java'

application {
    mainClassName = "Tickbot"
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'net.harawata', name: 'appdirs', version: '1.2.0'
    compile group: 'com.opencsv' , name: 'opencsv', version: '5.2'
}

run {
    standardInput = System.in
}